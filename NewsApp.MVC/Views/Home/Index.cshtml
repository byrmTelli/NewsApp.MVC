@{
    ViewData["Title"] = "DailyNews";
}

<partial name="navbarCategoriesSection"/>

<div class="text-center">
    <div class="row mt-2 items-center justify-content-center">
        <h5 class="text-start">Global</h5>
        <hr />
        @foreach (var newsItem in Model)
        {
            <div class="col-md-3">
                <div class="card-group mt-2">
                    <a asp-controller="Home" asp-action="Detail" asp-route-id="@newsItem.Id">
                        <div class="card">
                            <img src="@newsItem.Image" class="card-img-top" alt="...">
                            <div class="card-body">
                                <h5 class="card-title">@newsItem.Title</h5>
                                <p class="card-text"><small class="text-muted">@newsItem.Creator.Name @newsItem.Creator.Surname | @newsItem.CreatedAt</small></p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        }
    </div>
    <div class="row mt-2 items-center justify-content-center">
        <h5 class="text-start">Sports</h5>
        <hr />
        @foreach (var newsItem in Model)
        {
            if (newsItem.Category.Name == "Sports")
            {
                <div class="col-md-3">
                    <div class="card-group mt-2">
                        <a asp-controller="Home" asp-action="Detail" asp-route-id="@newsItem.Id">
                            <div class="card">
                                <img src="@newsItem.Image" class="card-img-top" alt="...">
                                <div class="card-body">
                                    <h5 class="card-title">@newsItem.Title</h5>
                                    <p class="card-text"><small class="text-muted">@newsItem.Creator.Name @newsItem.Creator.Surname  | @newsItem.CreatedAt</small></p>
                                </div>
                            </div>
                        </a>
                    </div>
                </div>
            }
        }
    </div>
    <div class="row mt-2 items-center justify-content-center">
        <h5 class="text-start">Politics</h5>
        <hr />
        @foreach (var newsItem in Model)
        {
            if (newsItem.Category.Name == "Politics")
            {
                <div class="col-md-3">
                    <div class="card-group mt-2">
                        <a asp-controller="Home" asp-action="Detail" asp-route-id="@newsItem.Id">
                            <div class="card">
                                <img src="@newsItem.Image" class="card-img-top" alt="...">
                                <div class="card-body">
                                    <h5 class="card-title">@newsItem.Title</h5>
                                    <p class="card-text"><small class="text-muted">@newsItem.Creator.Name @newsItem.Creator.Surname  | @newsItem.CreatedAt</small></p>
                                </div>
                            </div>
                        </a>
                    </div>
                </div>
            }
        }
    </div>
    <div class="row mt-2 items-center justify-content-center">
        <h5 class="text-start">Technology</h5>
        <hr />
        @foreach (var newsItem in Model)
        {
            if (newsItem.Category.Name == "Technology")
            {
                <div class="col-md-3">
                    <div class="card-group mt-2">
                        <a asp-controller="Home" asp-action="Detail" asp-route-id="@newsItem.Id">
                            <div class="card">
                                <img src="@newsItem.Image" class="card-img-top" alt="...">
                                <div class="card-body">
                                    <h5 class="card-title">@newsItem.Title</h5>
                                    <p class="card-text"><small class="text-muted">@newsItem.Creator.Name @newsItem.Creator.Surname  | @newsItem.CreatedAt</small></p>
                                </div>
                            </div>
                        </a>
                    </div>
                </div>
            }
        }
    </div>
</div>
